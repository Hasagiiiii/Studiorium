{
  "name": "lorion-v4",
  "version": "4.0.0-alpha.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:server": "npm run build --workspace @lorion/contracts && npm run build --workspace @lorion/domain && npm run build --workspace @lorion/database",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run build:server && npm run test --workspaces --if-present",
    "check": "npm run typecheck && npm run test",
    "smoke:deploy": "node scripts/predeploy-smoke.mjs"
  },
  "engines": {
    "node": "24.x"
  }
}
