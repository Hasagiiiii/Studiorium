name: Format editorial motion

on:
  push:
    branches:
      - fix/private-own-profile

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: fix/private-own-profile
      - uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: npm
      - run: npm ci --ignore-scripts --no-audit --no-fund
      - run: npx prettier --write public/js/animations/editorial-motion.js public/css/animations/editorial-motion.css tests/editorial-motion.test.js
      - shell: bash
        run: |
          git add public/js/animations/editorial-motion.js public/css/animations/editorial-motion.css tests/editorial-motion.test.js
          if git diff --cached --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "style(motion): apply project formatting"
          git push origin HEAD:fix/private-own-profile
